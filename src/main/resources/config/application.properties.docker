# Database configuration for Docker environment
# This file is used when running the application in Docker containers
# The values will be replaced with environment variables at runtime

database.url=jdbc:oracle:thin:@oracle-db:1521/XEPDB1
database.user=farm_manager
database.password=password
database.inet=oracle-db

# Sensor configuration (adjust paths for Docker volumes)
sensors.input.directory=/app/data/sensors/input
sensors.output.directory=/app/data/sensors/output
